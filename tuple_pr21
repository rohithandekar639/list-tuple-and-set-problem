# Create a tuple
my_tuple = (10, 45, 23, 67, 5, 87)

# Find the maximum value in the tuple
maximum_value = max(my_tuple)

# Print the maximum value
print("The maximum value in the tuple is:", maximum_value)
