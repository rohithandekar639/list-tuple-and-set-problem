# Input string
input_string = "Hello, World!"

# Initialize an empty tuple
character_tuple = ()

for char in input_string:
    character_tuple += (char,)

# Print the resulting tuple of characters
print(character_tuple)
