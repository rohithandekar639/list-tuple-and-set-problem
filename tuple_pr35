# Create a tuple with a mix of even and odd numbers
original_tuple = (1, 2, 3, 4, 5, 6, 7, 8, 9)

odd_numbers = tuple(x for x in original_tuple if x % 2 != 0)

# Print the tuple of odd numbers
print(odd_numbers)
