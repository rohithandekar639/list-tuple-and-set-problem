user_input = input("Enter a comma-separated list of values: ")

# Split the input string into a list using commas as the separator
input_list = user_input.split(',')

# Create a tuple from the list
user_tuple = tuple(input_list)

# Print the user-created tuple
print("User-created tuple:", user_tuple)
